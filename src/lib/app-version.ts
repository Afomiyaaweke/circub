/**
 * circub app version — shown in the footers on web, mobile web and the
 * Android app so anyone can confirm phone + desktop run the SAME build.
 *
 * ⚠️ Keep in sync with VERSION in public/sw.js (bump both on every deploy).
 */
export const APP_VERSION = 'v32';
